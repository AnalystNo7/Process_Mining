import { Empty, Typography } from 'antd';

export function AdminUsersPage() {
  return (
    <div>
      <Typography.Title level={3}>Пользователи</Typography.Title>
      <Empty description="Управление пользователями (UI задачи T06) — реализуется следующим" />
    </div>
  );
}
