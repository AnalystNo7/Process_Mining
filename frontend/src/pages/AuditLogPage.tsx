import { Empty, Typography } from 'antd';

export function AuditLogPage() {
  return (
    <div>
      <Typography.Title level={3}>Журнал действий</Typography.Title>
      <Empty description="Просмотр аудита (UI задачи T07) — реализуется следующим" />
    </div>
  );
}
